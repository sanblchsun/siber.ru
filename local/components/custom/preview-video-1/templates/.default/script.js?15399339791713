/*'use strict';
jQuery(document).ready(function($) {

	// Переход на следующий экран
	// ==========================
	$('#mouse-scroll').on('click', function(event) {
		event.preventDefault();
		window.app.listFirstScreen();
	});

	// Повтор видео
	// ==========================
	var video = document.getElementById('main-video');
	$('.replay').on('click', function(event) {
		video.play();
		setBtnTimeout();
	});
	var setBtnTimeout = function() {
		// setTimeout(function () {
		// 	$('.replay').addClass('show');
		// }, 10000);
	}
	setBtnTimeout();

	var showBtnReplay = function() {
		$('.replay').addClass('show');
	}

	video.addEventListener('ended', showBtnReplay, false);


	$('.audio').on('click', function(event) {
		$('.audio').toogleClass('active');
	});

	if (window.innerWidth < 768) {
        $('.video-container, .mobile-notify, .main-video').on('click', function(event) {
            video.play();
            $('.mobile-notify').css('display', 'none');
        });
	}


});


// Скролл первая-вторая страница
// =============================
window.addEventListener('scroll', onWindowScroll, false);
function onWindowScroll(event) {
	var scrollTop = app.$window.scrollTop();
	console.log(app.scrollLockStatus);
	// if (!app.scrollLockStatus) {
		var firstScreenActive = app.isFirstScreen();

		// переход на 2 экран
		var thisOffset = app.$window.scrollTop();

		if (firstScreenActive
			&& (thisOffset > app.lastOffset)
			) {
			app.listFirstScreen();
		}

		// if (firstScreenActive && (thisOffset < app.lastOffset)) {
		// 	app.listSecondScreen();
		// }
	// }

	if (scrollTop >= 0) {
		app.lastOffset = scrollTop;
	}
}*/